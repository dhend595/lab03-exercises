Find All Duplicates

Write a function (in python) or method (in Java) that accepts a list of integers and returns a list of only those integers that appear more than once

When we are using nested loops we are effectively running over every item in the array for every item in the array. My oneset approach instead uses a set to check for seen items and if they have been seen add them to our return. 
